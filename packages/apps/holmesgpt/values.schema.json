{
  "title": "Chart Values",
  "type": "object",
  "properties": {
    "host": {
      "description": "Hostname for external access via Ingress. Leave empty to skip Ingress.",
      "type": "string",
      "default": ""
    },
    "model": {
      "description": "LiteLLM-format model identifier (e.g. openai/qwen-7b, anthropic/claude-3-5-sonnet). Must be registered in the gateway pointed to by openaiBaseUrl, or be a public OpenAI / Anthropic model when calling those APIs directly.",
      "type": "string",
      "default": "openai/gpt-4o"
    },
    "openaiBaseUrl": {
      "description": "OpenAI-compatible API endpoint. Point this at an in-cluster LiteLLM gateway (e.g. http://litellm-\u003crelease\u003e.cozy-litellm.svc.cluster.local:4000/v1) to keep all SRE-related LLM calls inside the cluster. Empty falls back to the public OpenAI API.",
      "type": "string",
      "default": ""
    },
    "openaiApiKey": {
      "description": "Bearer token for the API endpoint. For LiteLLM, this is the master key. Stored as a Kubernetes Secret.",
      "type": "string",
      "default": ""
    },
    "replicaCount": {
      "description": "Number of HolmesGPT pods.",
      "type": "integer",
      "default": 1
    }
  }
}
